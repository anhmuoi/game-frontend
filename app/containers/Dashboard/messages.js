import { defineMessages } from 'react-intl';

export const scope = 'demasterpro.components.dashBoardPage';
export default defineMessages({
    msgNotiAllowLocation: {
    id: `${scope}.msgNotiAllowLocation`,
    defaultMessage: 'Turn on Precise Location to increase the accuracy of the forecast in your current location.',
  },
});
