import { defineMessages } from 'react-intl';

export const scope = 'demasterpro.containers.Common';
export default defineMessages({
  searchBox: {
    id: `${scope}.searchBox`,
    defaultMessage: `Search`,
  },
});
