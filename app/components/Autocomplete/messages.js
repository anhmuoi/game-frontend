import { defineMessages } from 'react-intl';

export const scope = 'demasterpro.components.Autocomplete';
export default defineMessages({
  notFound: {
    id: `${scope}.notFound`,
    defaultMessage: `Not Found`,
  },
  typeToSearch: {
    id: `${scope}.typeToSearch`,
    defaultMessage: `Type to search...`,
  },
});
