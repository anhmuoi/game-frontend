import { defineMessages } from 'react-intl';

export const scope = 'demasterpro.components.MultiSelectUI';
export default defineMessages({
  selectAll: {
    id: `${scope}.selectAll`,
    defaultMessage: `Select All`,
  },
  unselectAll: {
    id: `${scope}.unselectAll`,
    defaultMessage: `Unselect All`,
  },
});
